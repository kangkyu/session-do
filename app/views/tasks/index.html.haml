= render 'layouts/tab_bar', title: "Tasks in touch"

.row
  .col-md-5.col-md-offset-1.col-sm-6
    .text-center
      = render(@tasks.daily) || "You don't have any tasks to do."
  .col-md-5.col-sm-6
    .text-center
      = render(@tasks.put_off) || "You don't have any tasks to do."
